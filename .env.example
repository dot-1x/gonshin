# Base URL of the Hoyolab data API (FastAPI today, Go port later).
HOYOLAB_API_BASE=https://hoyo.dotcchix.dev

# HTTP listen address.
ADDR=:8080

# Cache TTL for cached (list) endpoints, in seconds. Character detail is never cached.
CACHE_TTL_SECONDS=1200
